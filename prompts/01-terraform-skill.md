Crée un skill Claude Code dans .claude/skills/terraform-gcp/SKILL.md avec le frontmatter name et description appropriés. La description doit indiquer de l'utiliser à chaque écriture ou édition de Terraform. Le corps doit tenir sur un écran et contenir, sous forme d'instructions directes : les défauts de sécurité (service account dédié par composant et least privilege jamais Owner/Editor, secrets via Secret Manager référencés en data source jamais en clair, bases en IP privée, buckets en accès uniforme non public, auth GitHub Actions via Workload Identity Federation sans clé exportée) et les règles de qualité (fmt et validate passent avant tout commit, versions de provider épinglées, variables typées avec defaults, outputs explicites, labels cohérents, aucune valeur en dur).