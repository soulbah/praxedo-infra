# Project Progress

Tracks the state of each phase. Source of truth — updated after every validated step.

Statuses: `todo` · `in-progress` · `blocked` · `done`

| # | Step | Status | Notes |
|---|------|--------|-------|
| 0 | Repo bootstrap (git init, .gitignore, terraform-gcp skill) | done | commit `1283fcd`, skill at `.claude/skills/terraform-gcp/SKILL.md` |
| 1 | Architecture scoping (`docs/architecture.md`) | done | validated, used as source of truth for §2 |
| 2 | Terraform layout & module skeleton (providers, backend, env stacks, root locals/labels) | done | root `terraform/` stack: pinned providers (`google` ~> 6.10, `google-beta` ~> 6.10, `random` ~> 3.6), GCS partial backend, workspace→env mapping (`dev`/`prod`) with `check` guardrail, common labels via provider `default_labels`, project-level API enablement, `envs/*.example.tfvars` + `envs/backend-*.example.hcl`, README with bootstrap + daily flow. `terraform fmt` clean, `terraform validate` passes |
| 3 | Foundation module — VPC, subnet, Cloud NAT, Serverless VPC connector, Artifact Registry | in-review | `modules/network`: VPC + /24 subnet (private Google access, flow logs), Cloud Router + manual static NAT IP (for AV whitelist), VPC Access connector /28, private services range + servicenetworking connection for Cloud SQL. `modules/artifact-registry`: one Docker repo (`praxedo-docker`) per env, cleanup policies (keep 10 tagged / drop untagged >7d), reader IAM for runtime SAs. `modules/service-accounts`: dedicated `praxedo-api` + `praxedo-scanner` SAs with self-signBlob binding on API SA |
| 4 | Storage module — `quarantine` + `clean` buckets, IAM split, lifecycle rules | in-review | `modules/storage`: UBLA + PAP enforced on both buckets; quarantine TTL 7d, no versioning; clean versioned with 30d noncurrent retention. IAM split enforcing §2.3: API SA = objectCreator on quarantine + objectViewer on clean; scanner SA = objectAdmin on quarantine + objectCreator on clean. `frontend-assets` bucket deferred to step 9 (edge module) where the LB/CDN signing model is decided |
| 5 | Database module — Cloud SQL PostgreSQL private IP, backups, PITR, users via Secret Manager | in-review | `modules/database`: PG15, `db-custom-1-3840`, private IP only (`ipv4_enabled=false`), SSL `ENCRYPTED_ONLY`, automated backups (7 retained) + PITR (7d tx-log), query insights on, maintenance window Sun 03:00, IAM auth flag off. App DB + user provisioned with password fetched from Secret Manager via data source. `deletion_protection=true` by default |
| 6 | Secrets module — Secret Manager entries + IAM bindings per consumer SA | in-review | `modules/secrets`: `praxedo-db-password` (TF-generated via `random_password` length=32) and `praxedo-av-api-key` (value seeded out-of-band by ops). User-managed replication pinned to `var.region`. `secretAccessor` granted least-privilege: DB password → API + scanner; AV key → scanner only (API SA cannot read vendor key) |
| 7 | Compute module — Cloud Run API service + scanner service, dedicated SAs, least-privilege IAM | in-review | `modules/compute`: both as `google_cloud_run_v2_service`. API ingress `INTERNAL_LOAD_BALANCER` (LB module later), VPC connector egress `PRIVATE_RANGES_ONLY` (Cloud SQL via private IP). Scanner ingress `INTERNAL_ONLY`, VPC egress `ALL_TRAFFIC` (AV via Cloud NAT static IP), timeout 600s aligned with Pub/Sub ack deadline. DB password + AV key mounted via `secret_key_ref`. `lifecycle.ignore_changes` on container image + Cloud Run client metadata so the app pipeline owns image promotion without TF drift |
| 8 | Eventing module — GCS finalize → Pub/Sub `scan-requests` + `scan-dlq` + scanner subscription | in-review | `modules/eventing`: `google_storage_notification` on quarantine → `scan-requests` topic (variance from arch §1.7 which named Eventarc — direct GCS notification keeps semantics identical while letting TF own the subscription knobs). Push sub: ack 600s, retry 10s-600s, max 6 attempts, DLQ → `scan-dlq`. Dedicated `praxedo-pubsub-inv` SA mints OIDC tokens for push (`run.invoker` on scanner only). Pub/Sub service agent granted token-creator on invoker SA, publisher on DLQ, subscriber on source sub. Pull subscription `scan-dlq-sub` for operator triage |
| 9 | Edge module — external HTTPS Load Balancer + Cloud CDN + managed cert for frontend bucket and API | in-review | `modules/edge`: global IP + managed SSL cert (var.domains) + URL map routing `/api/*` → Serverless NEG on Cloud Run API, default → CDN-fronted backend bucket. HTTP→HTTPS redirect. Frontend bucket: UBLA on, **PAP=`inherited` + `allUsers objectViewer` — scoped exception** required by arch §1.3 (anonymous LB origin reads for SPA); quarantine + clean buckets keep PAP=enforced so §2.3 invariant unchanged. SPA fallback via `website.not_found_page=index.html`. Cloud Run API gets `allUsers` invoker — safe because ingress=`INTERNAL_LOAD_BALANCER` restricts network reach to the LB |
| 10 | Observability module — log sinks, uptime checks, SLO alerts (5xx, scan latency, DLQ depth), notification channels | in-review | `modules/observability`: optional email notification channel (silent if `alert_email` unset). Three alert policies: API 5xx rate (req/s threshold), scan-requests oldest unacked age (>15 min default), scan-dlq-sub `num_undelivered_messages > 0`. Uptime checks deferred to step 9 (need public LB URL). Log sinks deferred — managed logging covers the SLOs at this scale |
| 11 | CI/CD enablement — Workload Identity Federation pool/provider, deploy SA, IAM bindings for app repo | in-review | `modules/cicd`: WIF pool `github-actions` + OIDC provider `github`, `attribute_condition` hard-restricts to `${github_owner}/${github_repo}`. Deploy SA `praxedo-app-deploy` with: `roles/artifactregistry.writer` repo-scoped, `roles/run.developer` project-scoped, `roles/iam.serviceAccountUser` on api+scanner SAs only, `roles/storage.objectAdmin` on frontend bucket. No JSON keys, federation only. Outputs `workload_identity_provider` + `app_deploy_sa_email` for app repo GH Actions consumption |
| 11b | Infra repo CI/CD pipeline — distinct from app pipeline (CLAUDE.md), WIF OIDC, no SA keys | in-review | `modules/infra-cicd`: dedicated WIF pool `infra-github` (separate from app pool — blast-radius isolation). Two SAs: `infra-plan` (read-only: viewer + iam.securityReviewer + secretmanager.viewer), `infra-apply` (explicit admin role list, no roles/owner\|editor per skill). Plan SA federable from any branch / PR; apply SA WIF binding pinned to `attribute.repository_ref == owner/repo@refs/heads/main` so branch-level lock is enforced at IAM. Both SAs IAM-bound to state bucket (objectAdmin — plan needs lock writes). Bootstrap (first apply by human owner) documented in runbook §8.2 + GitHub variable table |
| 11d | PR-specific quality gates — CODEOWNERS, draft-skip, PR title lint, drift detection | in-review | `.github/CODEOWNERS`: per-path review reqs (security group on storage/secrets/SAs/cicd/.github; architecture group on architecture.md+CLAUDE.md). `pr-title.yml`: Conventional Commits lint on PR title (squash-merge → commit message). checks workflow: skip draft PRs + fork PRs. `terraform-drift.yml`: daily 04:30 UTC `terraform plan` per env on main, opens/updates sticky issue `Terraform drift — <env>` on drift, Job Summary with full diff. Runbook §8.5 documents required branch protection rules (required status checks + CODEOWNERS-driven reviews) |
| 11c | Infra workflows — plan→approve→apply with frozen tfplan + GH Environment gate | in-review | `.github/actions/tf-setup`: composite action for install+WIF+init+workspace (single source of truth, used everywhere). `.terraform-version` pins CLI version. `terraform-checks.yml` (PR): fmt + validate + tflint + Trivy IaC scan (HIGH/CRITICAL fail) + plan dev/prod matrix → sticky PR comment + Job Summary with full diff + uploaded tfplan artifact (1d retention). `terraform-deploy.yml` (push main): per env `plan-X` → **GH Environment gate (manual approval)** → `apply-X` consuming frozen tfplan (`terraform apply tfplan`, no var-file, no re-plan = zero approval-vs-execute drift). `apply-prod` strict promotion (`needs: [plan-prod, apply-dev]`). `-detailed-exitcode` short-circuits apply when plan has no changes. Per-job permissions scoped (`contents: read` workflow-wide, `id-token: write` only on jobs needing WIF, `pull-requests: write` only on the comment step). Fork PRs excluded from OIDC-bearing jobs. Job timeouts set everywhere. Old `terraform-apply-{dev,prod}.yml` superseded |
| 12 | Environment wiring — `dev` and `prod` stacks consuming the modules with env tfvars | in-review | `envs/dev.example.tfvars` + `envs/prod.example.tfvars` extended: dev ZONAL+deletion_protection=false, prod REGIONAL+HA+higher max instances+`alert_email` required. Domains, github_owner/repo per env |
| 13 | Pre-commit & repo hygiene — terraform fmt/validate hooks, tflint/tfsec, README per module | in-review | `.pre-commit-config.yaml`: trailing-whitespace, EOF, yaml, merge-conflict, private-key, large-files (512 KB cap) + `terraform_fmt`, `terraform_validate`, `terraform_tflint`, `terraform_trivy` (HIGH/CRITICAL). `terraform/.tflint.hcl` with google ruleset. `Makefile` wraps init/fmt/validate/plan/apply/lint per env |
| 14 | End-to-end validation — `terraform plan` per env, manual smoke checklist, document operational runbook | in-review | `docs/runbook.md`: bootstrap, daily flow, AV key seeding, domain/cert provisioning, app pipeline handoff, recovery paths for the 3 architecture failure modes (AV slow / AV down / §2.3 invariant verification), operator commands, pre-commit, scoped exception ledger. `terraform fmt`/`validate` pass on every commit. Live `terraform plan` per env still pending — needs real GCP project + state bucket bootstrap |
