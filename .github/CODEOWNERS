# 3-dev team, no separate security/architecture roles — single team
# reviews everything. CODEOWNERS still wired in because branch protection
# can require codeowner review, which keeps a self-merge of a sensitive
# IAM change off the table even on a quiet afternoon.

* @praxedo/infra
